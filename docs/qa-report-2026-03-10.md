# QA Report - 2026-03-10

Pre-production QA run for the ticket batch TKT-02 to TKT-12.

## Automated checks

Command:

```bash
python3 scripts/qa_check.py
```

Result:

```text
[PASS] Content JSON is valid
[PASS] All required route files exist
[PASS] Robots, sitemap and custom 404 files exist
[PASS] No legacy 2024 residue in program pages
[PASS] Legal links include privacy policy and regulation PDF
[PASS] Contact email format is valid
[PASS] Main navigation structure looks correct
[PASS] SEO meta markers are present on public pages and admin is noindexed
[PASS] robots.txt contains admin disallow and sitemap
[PASS] sitemap.xml includes all public routes
[PASS] All local media assets referenced in content exist
[PASS] Critical content/assets no longer hotlink the source site
[PASS] Netlify headers include security, admin noindex and content no-store
[PASS] Submission config forwards allowedEmails for admin gating
[PASS] QA checks completed successfully
```

## Route smoke checks

Routes verified locally with HTTP 200:

- `/`
- `/digital-in-pulse/`
- `/le-principe/`
- `/tech-for-competitivity/`
- `/women-for-innovation/`
- `/mentions-legales/`
- `/politique-relative-a-lutilisation-des-cookies/`
- `/conditions-generales-dutilisation/`
- `/politique-de-confidentialite/`
- `/admin/`

## Residual risks

- Candidatures + fichiers sont stockes via GitHub API: acceptable pour ce setup actuel, mais moins adapte qu'un stockage dedie si volume/contraintes RGPD augmentent.
- Validation finale en domaine reel reste obligatoire (publish admin + soumission test + export CSV).
